
use tcw3::{ui::views::Label, pal};

#[widget]
pub comp stella2::TestWidget {
    pub prop text: String = || "hello!".to_string();

    on (init) |this.text| { dbg!(); }

    on (this.text) |this.text| { dbg!(); }

    // TODO: support reading event data
    // on (this.some_event) |this, event.wm| { dbg!(); }

    pub event some_event(wm: pal::Wm);

    const child = Label {
        prop text = |this.text| format!("text = '{}'", text);
    };
}
