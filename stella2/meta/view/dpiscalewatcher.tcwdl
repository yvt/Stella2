use tcw3::{uicore::{HView, HWnd}, pal};

#[widget]
#[prototype_only]
#[builder(simple)]
pub(crate) comp crate::view::dpiscalewatcher::DpiScaleWatcher {
    const subview: HView { pub get; pub set; }

    const view: HView { pub get; } = unreachable!();

    pub event dpi_scale_changed(wm: pal::Wm, hwnd: HWnd);
}
