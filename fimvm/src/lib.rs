//! fimVM (Fast, Interpreted, Minimum Virtual Machine), a virtual machine
//! for integration with the translation system.

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
