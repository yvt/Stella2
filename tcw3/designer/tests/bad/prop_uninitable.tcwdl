pub comp crate::Comp1 {
    const wm: tcw3_pal::Wm { pub set; }

    // `field` has no way to initialize
    prop field: u32 {}
    //~^ ERROR Must have a default value or a setter because otherwise it's impossible to initialize
}
