use crate::uicore::{SizeTraits, HView};

/// A `Layout` that overlaps a subview to fill the owning view.
#[prototype_only]
#[builder(simple)]
pub comp crate::ui::layouts::FillLayout {
    const subview: HView { pub set; }

    // TODO: margin
}
